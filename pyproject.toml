[project]
name = "python-uv-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gradio[mcp]>=5.30.0",
    "mcp[cli]>=1.13.1",
]
